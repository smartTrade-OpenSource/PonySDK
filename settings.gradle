plugins {
    // Auto-provisions the JDK declared by the Java toolchain (see build.gradle)
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
}

include 'ponysdk','ponysdk-spring','sample'